[server]
# API 服务监听地址
host = 127.0.0.1
# API 服务监听端口
port = 8000

[file_storage]
# 文件存储根目录（相对于 api.py 所在目录，或绝对路径）
base_dir = static/file
# 文件保留天数，0 = 永久保留
retention_days = 90
# 过期文件清理间隔（秒）
cleanup_interval = 3600
